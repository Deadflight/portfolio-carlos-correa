import React, { FC, PropsWithChildren } from "react";
import "../globals.css";

const ContactLayout: FC<PropsWithChildren> = ({ children }) => {
	return <>{children}</>;
};

export default ContactLayout;
